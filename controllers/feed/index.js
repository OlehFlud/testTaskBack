const createFeed = require('./createFeed');
const deleteFeed = require('./deleteFeed');
const getFeedById = require('./getFeedById');
const findAll = require('./findAll');

module.exports = {
    createFeed,
    deleteFeed,
    getFeedById,
    findAll
};
