const checkIsFeedPresentMiddleware = require('./check-is-feed-present-middleware');
const checkFeedValidityMiddleware = require('./check-feed-validity-middleware');

module.exports = {
    checkIsFeedPresentMiddleware,
    checkFeedValidityMiddleware
};
